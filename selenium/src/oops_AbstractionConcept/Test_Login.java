package oops_AbstractionConcept;

public class Test_Login {
 public void doLoginid(WebBrowser browser) {
	 browser.searchElement().sendKeys("admin");
	 browser.searchElement().sendKeys("admin");
	 browser.searchElement().click();
 }
}
